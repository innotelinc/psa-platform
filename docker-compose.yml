services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: psa-platform
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - psa-data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped

volumes:
  psa-data:
